{
  "schema_version": 1,
  "domain": "suite",
  "description": "Campos de SuitePackage.toSyncJSON() — projecao de sync desktop ↔ mobile",
  "required": ["id", "package_id", "status", "module_type"],
  "fields": {
    "id": { "type": "string" },
    "package_id": { "type": "string" },
    "version_no": { "type": "number" },
    "module_type": { "type": "string" },
    "resource_type": { "type": "string" },
    "status": { "type": "string", "enum": ["pending_review", "approved", "refuted", "current", "closed", "dispatched"] },
    "owner_id": { "type": "string" },
    "is_current": { "type": "number", "enum": [0, 1] },
    "locked_by": { "type": "string", "nullable": true },
    "locked_at": { "type": "string", "nullable": true },
    "ref_package_id": { "type": "string", "nullable": true },
    "ref_package_ver": { "type": "number", "nullable": true },
    "entity_id": { "type": "string", "nullable": true },
    "entity_type": { "type": "string", "nullable": true },
    "payload_json": { "type": "string", "nullable": true },
    "created_at": { "type": "string" },
    "closed_at": { "type": "string", "nullable": true }
  }
}
